from django.conf.urls import url
from profiles import views

urlpatterns = [
    #url(r'^(?P<api>[^/]+)/$', views.view_profile, name='view'),
    #url(r'^edit/(?P<api>[^/]+)/$', views.edit_profile, name='edit'),
]
